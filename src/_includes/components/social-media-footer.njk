{#
Social Media Footer Component

Social media links in footer.

Props:
- navigation.social: Object containing social media platforms

Usage:
#}

<div class="social-media-footer">
  {% for platform_key, platform in navigation.social %}
    <a href="{{ platform.url }}" 
       target="_blank" 
       rel="noopener noreferrer"
       class="social-link"
       aria-label="Follow us on {{ platform.name | default(platform_key)}} (opens in new window)">
      <img src="{{ platform.icon }}" 
           alt="{{ platform.name | default(platform_key)}} icon"
           class="social-icon">
    </a>
  {% endfor %}
</div>